this project needs Python version 3.6.9 

run the command "uvicorn main: app --reload" in the backend folder to start the server

run the command "npm start" in the frontend folder to start the server